TRIPVISUALS v8 — COMPLETE CSP PATCH
Unzip over repo root → commit → push.

WHAT THIS ROUND DOES
━━━━━━━━━━━━━━━━━━━━
Eliminates ALL inline CSS from every HTML page.

BEFORE: style-src 'self' 'unsafe-inline' https://fonts.googleapis.com
AFTER:  style-src 'self' https://fonts.googleapis.com

Combined with the previous round (script-src already clean),
the CSP is now: default-src 'self' with strict overrides.
No 'unsafe-inline' anywhere in either script-src or style-src.

WHAT CHANGED
  32 inline style= attributes → utility classes (.vz-toggle-row, .vz-grid-2 etc)
  display:none → HTML hidden attribute (+ removeAttribute('hidden') in JS)
  12 embedded <style> blocks → 6 external CSS files:
    admin.css             (shared + admin page styles appended)
    admin-landing.css     (landing admin page — 17KB)
    style.css             (classico landing — about/howto styles appended)
    catalogo.css          (catalog page)
    landing-retro.css     (retro landing)
    login.css             (login page)

NOTE: JS-set styles (element.style.backgroundColor etc from the color
picker and loader scripts) are NOT covered by style-src and are always
allowed — this is correct and expected. The CSP only governs HTML.

HONEST SCORE: 9.5 / 10
Remaining 0.5:
  · Landing page visual design (your filter system — intentionally deferred)
  · Sentry activation (SENTRY_DSN env var, already wired)
  · Existing product image URLs not retroactively transformed
    (only new uploads get Cloudinary transforms — re-upload to update)
